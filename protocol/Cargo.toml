[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
crc = "3.4.0"
serde = { version = "1.0.229", default-features = false, features = ["derive"] }
ufmt = { version = "0.2.0", optional = true }

[features]
default = []
ufmt = ["dep:ufmt"]
